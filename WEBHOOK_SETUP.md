# 🔗 Налаштування Webhook для Render

## Що змінилося:

Бот тепер використовує **webhook** замість polling, що дозволяє працювати на Render free без обмежень.

## Кроки після деплою на Render:

### 1. Після успішного деплою

Коли твій web service запуститься на Render, скопіюй його URL:
- Наприклад: `https://pon-school-bot.onrender.com`

### 2. Додай змінну оточення WEBHOOK_URL

На Render → твій Web Service → Environment:
- Додай: `WEBHOOK_URL` = `https://pon-school-bot.onrender.com`
- (БЕЗ слеша в кінці!)

### 3. Перезапусти сервіс

Після додавання змінної Render автоматично перезапустить сервіс, і webhook встановиться автоматично.

### 4. Або встанови webhook вручну

Якщо автоматичне встановлення не спрацювало, виконай в браузері:

```
https://api.telegram.org/bot<ТВІЙ_BOT_TOKEN>/setWebhook?url=https://pon-school-bot.onrender.com/webhook/<ТВІЙ_BOT_TOKEN>
```

Заміни `<ТВІЙ_BOT_TOKEN>` на реальний токен.

### 5. Перевірка

Відкрий в браузері:
- `https://pon-school-bot.onrender.com/health` - має показати `{"status":"ok"}`
- `https://pon-school-bot.onrender.com/` - має показати статус бота

## Переваги webhook:

✅ Працює на Render free без обмежень  
✅ Не потребує постійного polling  
✅ Швидша відповідь на повідомлення  
✅ Стандартний спосіб для Telegram ботів  
✅ Render бачить відкритий порт → free працює  

## Для локального тестування:

Для локального тестування `WEBHOOK_URL` не потрібен. Бот працює, але webhook не встановиться автоматично (це нормально для локального тестування).

---

**Готово! Бот працює через webhook на Render free! 🚀**
